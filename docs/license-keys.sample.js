/**
 * Copy this file to docs/license-keys.js and replace the example hashes with
 * the salted SHA-256 strings generated for your real unlock codes.
 *
 * Each entry should be produced with the helper script:
 *   node scripts/hash_unlock_code.js "YOUR-CODE-HERE"
 *
 * Keep docs/license-keys.js out of version control so your customer codes are
 * not exposed publicly. The app will load this module (if it exists) at runtime.
 */
export const LICENSE_HASHES = [
  // "replace-this-with-a-real-hash"
];
