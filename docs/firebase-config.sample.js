/**
 * Copy this file to docs/firebase-config.js and replace the values
 * with your Firebase project's credentials (Project settings →
 * General → Your apps). The object must be available on window
 * before app.js executes.
 */

window.FIREBASE_CONFIG = {
  apiKey: 'YOUR_API_KEY',
  authDomain: 'YOUR_PROJECT.firebaseapp.com',
  projectId: 'YOUR_PROJECT_ID',
  storageBucket: 'YOUR_PROJECT.appspot.com',
  messagingSenderId: 'YOUR_SENDER_ID',
  appId: 'YOUR_APP_ID',
};
